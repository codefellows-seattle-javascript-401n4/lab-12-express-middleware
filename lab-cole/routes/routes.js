'use strict';

const express = require('express');
const parser = require('body-parser');
const routers = module.exports = express.Router();

routers.get('', (req, res, next) => {
    //code stuff
    //.then
    //.catch
});

routers.post('', (req, res, next) => {
    //code stuff
    //.then
    //.catch
});

routers.put('', (req, res, next) => {
    //code stuff
    //.then
    //.catch
});

routers.delete('', (req, res, next) => {
    //code stuff
    //.then
    //.catch
});